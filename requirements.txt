# ══════════════════════════════════════════════════════════════════
#  《三国·蜀汉突围》依赖声明
# ══════════════════════════════════════════════════════════════════
#  本工程为零第三方依赖项目：游戏本体、配置加载器、存档模块与测试体系
#  全部只使用 Python 标准库（os / sys / time / json / logging / argparse /
#  importlib / io / contextlib / tempfile / unittest / py_compile / re）。
#
#  因此：
#    · 不需要 create venv 之外的任何安装步骤；
#    · `python -m pip install -r requirements.txt` 可安全执行（不会安装任何包）；
#    · 架构测试 tests/test_architecture.py 会逐行扫描生产代码，
#      确保没有任何第三方 import 混进来（这条约束是被自动校验的，不是口头承诺）。
#
#  开发与测试环境：Python 3.13.1（Windows 11）
#  未做多版本兼容矩阵验证，故不声明具体最低版本；如需用于旧解释器请自行实测。
