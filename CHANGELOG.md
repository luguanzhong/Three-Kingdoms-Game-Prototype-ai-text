# 更新日志 —— 三国 · 蜀汉突围

本项目采用**简化版 Keep a Changelog** 结构（新增 / 变更 / 修复 / 测试），版本号沿用代码头部标注。

> **历史来源说明**：本仓库的 Git 历史建立于 M1 阶段（2026-08-19），因此 v0.3.0 及更早的条目**不是**从 `git log` 提取，而是根据项目自带的《开发AGENT_LOG.md》（逐次真实记录）与仓库内保留的三个历史版本快照整理，未编造日期、功能或修复项。
> **版本号说明**：早期两次迭代在日志中曾叫 v1.0 / v1.1，自 v0.2.0 起统一为 v0.x 命名，本文件按当时的叫法如实记录。

---

## [未发布 · M6 仓库规范化] — 2026-09-17

> **本次为工程整理改动**：游戏规则版本仍为 v0.3.0，**未改动任何判定逻辑、数值平衡与玩法内容**；
> 改动全部落在目录结构、入口与工程配置上，改后 126 项自测断言与 74 个用例全部通过。

### 新增
- **根目录统一入口 `main.py`**：把 `src/` 加入模块搜索路径后转发到 `主函数()`，
  因此 `python main.py <参数>` 与 `python src/蜀汉突围.py <参数>` 完全等价；`--help` 的使用说明按实际入口显示
- `requirements.txt`：显式声明零第三方依赖
- `.gitattributes`：统一换行符策略（仓库内一律 LF），消除 CRLF 噪音警告
- `docs/images/.gitkeep`：为 README 的截图占位预留目录
- `legacy/` 目录：收纳三个历史版本快照

### 变更
- **目录规范化**：核心代码 `蜀汉突围.py` / `config_loader.py` / `save_manager.py` → `src/`；
  开发与历史文档 → `docs/`；历史版本快照 → `legacy/`；`三国.txt` → `docs/游玩实录.txt`
- 路径推导同步改为「项目根目录 = `src/` 的上一级」：`config/` 与 `saves/` **位置不变**，
  因此在任意工作目录下运行的结果与整理前完全一致
- `.gitignore` 重写为严格版：Python 缓存、虚拟环境、打包产物、测试与检查工具缓存、IDE、
  操作系统文件、临时与备份文件、日志、玩家存档
- README 全文重写：补「AI 辅助开发说明」章节、项目结构树、机制清单、截图与演示视频占位、测试验证结果
- 测试夹具 `tests/夹具.py`：生产模块改为按 `src/` 相对路径加载；`自测_确定性判定.py` 仅新增 3 行
  `src/` 路径引导，**126 项断言与用例逻辑零改动**
- 架构用例的白名单凭据改为在 `legacy/` 中查找历史快照（断言强度不变）

### 修复
- **干净环境实测发现的缺陷（用临时 venv 复现）**：`requirements.txt` 原本使用 UTF-8 中文注释，
  而 `pip` 按系统区域编码（中文 Windows 为 GBK）读取该文件，导致 `pip install -r requirements.txt`
  直接抛 `UnicodeDecodeError` 并以退出码 2 失败；现已改为纯 ASCII（约定：源码保留中文注释，
  但**交给工具读取的文件必须 ASCII**）
- **日志重定向乱码**：`sys.stdout` 已设为 UTF-8，而 `sys.stderr` 未设置，导致
  `python main.py --demo --log-level INFO > log.txt` 重定向出来的日志按 GBK 落盘、在 UTF-8 环境中显示为乱码；
  已同步为 stderr 也设置 UTF-8（仅影响输出编码，不涉及任何判定逻辑）
- 更正本文件 v1.0.0 条目的用例总数笔误（73 → 74；`v1.0.0` 标签下实测为 74 个用例）

### 测试
- **干净环境验证**（新建临时 venv，其中只有 `pip`，且全部命令都在无关工作目录 `C:\` 下执行）：
  - `python -m pip install -r requirements.txt` → 退出码 0，**未安装任何包**
  - `python run_tests.py` → **74 个用例通过**，81.6 秒
  - `python -m pytest tests -q` → **74 passed**，81.64 秒（额外装 pytest 仅为验证兼容性，
    项目本身**不依赖** pytest，见 `requirements.txt`）
  - `python main.py --demo --log-level INFO` → 退出码 0，跑满 20 回合达成「汉家尚有可为」结局，
    1223 行输出与 22 行 INFO 日志均无编码异常
  - 跨进程存档往返：进程 A 改兵力/粮草/日期/将领任务后保存 → 进程 B 全新解释器读档 →
    兵力、粮草、日期、任务四项全部一致
- 本机（非 venv）复测：`python run_tests.py` → **74 个用例通过**，81.9 秒；
  `--fast` → **70 个用例通过**，10.9 秒；`python 自测_确定性判定.py` → **全部 126 项自测通过**
- 命令行抽查：`--version` / `--check-config` / `--list-saves` / `--help` 全部正常

---

## [v1.0.0] — 2026-09-13（M5 发布与演示）

> **版本口径**：本条目是**工程发布版本 v1.0.0**，对应本地标签 `v1.0.0`；游戏规则版本仍为 **v0.3.0**
> （M2–M5 均未改动判定逻辑与数值平衡）。这是仓库首个正式发布点。

### 新增
- **argparse 命令行入口**（标准库，仅在 `__main__` 中解析，import 本模块不会读取 argv）：`--version`、`--help`、`--load/-l [存档名]`、`--demo`、`--list-saves`、`--check-config`、`--config <目录>`、`--log-level <级别>`；原有"不带参数即新开局"与游戏内第 7 项存档菜单保持不变
- **标准库 logging**：分层记录器 `蜀汉突围` / `蜀汉突围.配置` / `蜀汉突围.存档`，输出到 stderr，默认 `WARNING`；`--log-level INFO` 记录启动（发布/规则版本、是否读档）、配置加载结果、每回合关键状态、每次存档/读档与结局
- `--demo` 无人值守演示入口（等价交互模式输入 `2`）；`主循环(载入存档名=None, 自动演示=None)` 新增可选参数，默认行为不变
- `DEMO.md`：2~3 分钟演示脚本（时间盒、可复制命令、演示要点、备用问答）
- `docs/架构图.md`：分层图 + 回合 13 步流水线 + 四条结局路径（Mermaid 图与纯文本版并存，附逐层讲解提示）

### 变更
- `--config` 指定配置目录时一并重算启动期派生量 `险关位置`
- README 增加命令行参数表、日志说明、可复现运行步骤（三命令上手）、演示入口与架构图链接
- 零随机架构检查白名单更新：允许标准库 `logging` / `argparse`；关键字扫描改为"长词优先 + 词边界"，并排除 `choices=` 这类关键字参数写法——新增 1 项自检用例证明白名单不会放过真实随机调用

### 修复
- 无（未改判定逻辑；M5 期间架构用例先报出上述两处白名单适配问题，已一并记录在"变更"中）

### 测试
- `python run_tests.py` → **74 个用例通过**（含旧自测 126 项断言全量回归）；`python run_tests.py --fast` → **70 个用例通过**
  （注：本条目原写成 73 个，属成稿时的笔误，已在 M6 条目中更正为实测值 74）

### 可复现运行步骤
1. 环境：Python 3.7+（验证于 3.13.1），无需安装任何依赖；
2. 运行：`python main.py`（新开局）/ `python main.py --demo`（自动演示）/ `python main.py --load save1`（续局）；
3. 测试：`python run_tests.py --fast`（约 11 秒）或 `python run_tests.py`（约 85 秒）；
4. 演示：按 `DEMO.md` 走 2~3 分钟流程。

---

## [未发布 · M4 测试工程化] — 2026-09-13

> 本次为**工程化改动**：游戏规则版本仍为 v0.3.0，生产代码的判定逻辑与数值未做任何改动（本提交只新增测试与文档）。

### 新增
- `tests/` 分类测试目录（unittest，标准库）：`test_rules.py`（规则 23 例）、`test_boundaries.py`（边界 20 例）、`test_endings.py`（结局 7 例）、`test_regressions.py`（回归 10 例）、`test_architecture.py`（架构 8 例）、`test_legacy_suite.py`（旧自测包装 4 例）、`夹具.py`（公共夹具）
- `run_tests.py` 一键测试入口：`python run_tests.py` 跑全部用例并输出 总数/通过/失败/错误/跳过/用时，退出码可用于 CI；`--fast` 跳过耗时的旧自测全量回归（约 11 秒）
- 零随机架构检查升级：由原先 3 条"子串是否存在"断言，扩展为对三个生产模块**逐文件逐行**匹配 13 个随机关键字（random/randint/randrange/shuffle/choice/choices/uniform/sample/getrandbits/seed/secrets/urandom），命中时报 `文件:行号: 内容`；另加"不得导入随机库""仅标准库依赖白名单""同输入结果稳定""两次加载初始状态一致"四项检查
- 10 项回归用例，逐一对应真实修复或高风险边界：M2 非法配置整体回退/报错文案/拼写错误、M3 存档被误判损坏（城池必填字段差异）/损坏存档返回失败不改局面/存档名非法字符/原子写入无残留、练兵任务产出语义、冬季惩罚分档依据

### 变更
- `自测_确定性判定.py` **未改动**（126 项断言一项不删、仍可单独运行），改由 `tests/test_legacy_suite.py` 整体执行并核对通过与断言数基线
- README 新增「测试方法（M4 测试工程化）」：一键命令、分类表、回归覆盖表、新增回归用例流程、零随机检查与白名单说明

### 修复
- 无（本次不改生产代码；测试自身修正了两处用例写法：方法名不得含空格、避免用受一致性约束的字段测范围端点）

### 测试
- 一键测试：`python run_tests.py` → **73 个用例全部通过**（含旧自测 126 项断言全量回归），用时约 82 秒；`--fast` → 69 个用例全部通过，约 11 秒

---

## [未发布 · M3 存档 / 读档] — 2026-09-13

> 本次为**工程化改动**：游戏规则版本仍为 v0.3.0，所有数值与判定逻辑逐位未变。

### 新增
- `src/save_manager.py`：状态采集与恢复（模块全局命名空间 ↔ JSON 字典）、结构校验（必填字段 / 类型 / 范围）、版本判定、原子写入（`.tmp` + `os.replace`）、存档列表与逐档状态检查
- `saves/` 存档目录（运行时自动创建，已在 `.gitignore` 中排除）；存档为可读 JSON，顶层含 `存档版本`（当前 1）、`游戏版本`、`保存时间`、日期、三方势力、敌方城池、蜀汉城池、将领们、情报、全局
- 游戏内入口：决策菜单新增第 `7` 项【存档 / 读档】（保存 / 读取 / 列出，轻量操作，不占"大事"名额）
- 命令行入口：`python main.py --load <存档名>` 续局、`--help` 查看用法；`python src/src/save_manager.py` 列出存档
- 自测新增 **22 项**存档用例：菜单存档落盘、保存不改状态、读档往返指纹一致、续局结果与不中断的一局完全一致、非法 JSON / 缺字段 / 类型错 / 将领字段错 / 文件不存在 / 旧版本 / 更新版本 / 非法存档名 / 读档失败不改状态 / 列表区分损坏 / 模块零随机且仅用标准库

### 变更
- `src/蜀汉突围.py` 新增 `游戏版本` 常量（写入存档）；`主循环(载入存档名=None)` 支持启动即续局；`运行对局` 测试辅助支持透传主循环参数
- stdin 关闭（管道结束）时干净退出并给出续局提示，不再抛 traceback

### 修复
- 存档校验把"敌方城池"的必填字段 `守军` 误用到"蜀汉城池"（后者用 `兵力`），导致所有存档被判为损坏、无法读取——由新增测试发现，已按区块分别约定必填字段

### 测试
- 原有 104 项断言全部通过；新增存档用例后共 **126 项全部通过**

---

## [未发布 · M2 配置数据化] — 2026-09-13

> 本次为**工程化改动**：游戏规则版本仍为 v0.3.0，所有数值与判定逻辑逐位未变。

### 新增
- `config/` 目录与五份配置文件：`units.json`（兵种/精锐比例/募兵）、`schemes.json`（计谋条件与效果）、`seasons.json`（季节月份与冬季惩罚）、`diplomacy.json`（势力值与外交阈值）、`battle.json`（战斗倍率与各类系数）
- `src/config_loader.py`：读取 + 与内置默认值深度合并 + 校验（类型 / 范围 / 枚举 / 引用完整性 / 顺序一致性 / 季节月份覆盖 / 未知字段）；内置默认值与抽离前的硬编码数值逐位一致，因此可整体删除 `config/` 按原行为运行；支持 `python src/src/config_loader.py` 单独自检
- 自测新增 **21 项**配置用例：默认值与内置默认一致、配置目录不存在/为空时回退、类型错/范围越界/枚举非法/引用不存在/顺序矛盾/季节覆盖不全/字段拼写错误逐项报错、非严格模式整体回退、只改配置即改变判定行为、加载器零随机与仅用标准库

### 变更
- `src/蜀汉突围.py` 由写死数字改为读配置：有效兵力系数、强攻/围攻/必败/破城倍率、进攻损失与收编系数、防御加成、守军比例、曹魏与东吴入侵系数、死士突袭、北伐大捷结算、计谋前置条件与效果、冬季粮草与冻伤、季节月份映射、镇守子任务数值、东吴均势外交阈值与背刺参数、势力值权重
- 主循环启动时若配置有问题，打印【配置警告】（指出文件、字段、期望值、实际值）并使用内置默认值继续运行，不中断游戏

### 修复
- 非严格模式改为**整体回退**内置默认值：此前"部分合并"会让非法值进入判定逻辑（由本次新增测试发现并修复）

### 测试
- 原有 83 项断言全部通过；新增配置用例后共 **104 项全部通过**

---

## [v0.4.0-baseline] — 2026-08-19（M1：仓库与文档基线）

### 新增
- `README.md`：项目定位、运行与测试方法、核心规则速览、目录说明、迭代历史、架构简述、已知限制
- `CHANGELOG.md`：本文件
- `docs/架构说明.md`：三层模块结构、回合数据流、零随机约束、4 条结局路径、测试体系、改动指引
- `docs/规则说明.md`：初始数值、将领与任务、军事阈值、计谋前置条件、13 步结算、阈值事件、冬季、东吴 AI、结局速查
- `.gitignore`：排除非本项目目录 `地图/`、Python 缓存与编辑器文件
- 建立本地 Git 仓库并创建标签 `v0.4.0-baseline`

### 变更
- 无代码变更。

### 修复
- 无。

### 测试
- `python 自测_确定性判定.py` → 全部 83 项自测通过（与 v0.3.0 一致，覆盖未削弱）。

---

## [v0.3.0] — 2026-08-19

### 新增
- **月份回合推进系统**：全局量 `当前年份` / `当前月份` / `回合计数` / `季节`（开局建安十三年九月·秋季）；`中文数字()`、`显示日期()`、`推进日期()`（月份 +1、跨年进位、季节自动推算）；首次进入十二月的【冬至】提醒（仅一次，不影响数值）。
- **冬季确定性惩罚**：粮草消耗 ×1.5（有军队在外）或 ×1.25（退守后方）；兵力冻伤 -2% 或 -1%；`有进攻行为判定()` 判定依据为存在"北伐先锋"任务将领或围攻进行中。
- **东吴均势外交 AI**：`蜀汉控制城池数()`、`计算势力值()`（兵力 + 城池数×3000 + 将领总统御×200）、`东吴AI更新()`；亲魏（观望 +3、好感 -2、观望>80 密使通魏、>90 魏吴盟约）/ 亲蜀（观望 -2、好感 +3、好感>70 提出结盟由玩家抉择）/ 观望（观望 -1、好感 +1）三态。
- 全局状态新增：`东吴外交倾向`、`东吴结盟对象`、`东吴主动接触标记`、`自动模式`、`冬至提醒已触发`。

### 变更
- 近事日志前缀由"第 X 回合｜"改为"【建安X年X月】"；局势面板标题改为"建安十三年九月 ｜ 第 1 / 20 回合"，东吴栏显示外交倾向，情报栏显示季节。
- 东吴背刺触发条件由"观望态度 >80"改为"外交倾向为亲魏 且 观望 >85"。
- `结局判定()` 改用 `回合计数` 判断 20 回合上限；`主循环()` 每回合结束推进日期；`自动策略()` 改为魏延与黄忠同时练兵。
- 移除全局字典中的 `当前回合` 键（由模块级 `回合计数` 取代）。

### 修复
- 无（自测用例自身的期望值错误已修正，非游戏缺陷）。

### 测试
- 自测由 51 项扩展到 **83 项**：新增月份推进/跨年/季节更替、冬季全额与减半惩罚、势力值与三态外交、背刺门槛（观望不背刺 vs 亲魏背刺）等断言。

---

## [v0.2.0] — 2026-08-18

### 新增
- **ASCII 战区态势图**：`绘制战区态势图()` 与 `任务简称()`、`蜀城将领文本()`、`魏城守将文本()`；在"近事"之后渲染，按归属动态显示【魏】/【蜀】前缀、驻将与任务、险关★、东线要塞◆、首都洛阳与图例。
- **曹魏首都洛阳**（守军 12000 / 粮草 80 / 守将曹真 / 首都标记）与**北伐大捷结局**；`洛阳可攻判定()` 解锁条件为宛城或樊城归蜀，未解锁时提示"北伐之路未通，需先取宛城或樊城以打通粮道。"
- **蜀汉新城池"永安"**（兵力 3000 / 守将赵云 / 东线要塞）与**新将领赵云、黄忠**。

### 变更
- 初始兵力与结局判定基准由 15000 调整为 **18000**（含永安 3000）。
- `攻占城池()`：洛阳走特殊结算（曹魏兵力 ×0.5、东吴观望归零、蜀汉兵力 +5000）并跳过常规连锁。
- `结算回合()`：洛阳陷落后曹魏不再主动进攻。

### 修复
- 无。

### 测试
- 自测由 37 项扩展到 **51 项**：新增洛阳数据、解锁条件、北伐大捷结算与结局优先级、态势图渲染等断言。

---

## [v1.1] — 2026-08-18（早期内部版本号，后统一为 v0.x）

### 新增
- **精锐士兵系统**：`蜀汉["精锐比例"]`（0~100）、`有效兵力计算()`（有效兵力 = 兵力 ×(1+精锐比例/100)）。
- **镇守城池子任务**：将领新增 `子任务` 字段（无 / 练兵 → 精锐比例 +5 / 募兵 → 兵力 +200、粮草 -20）。
- 曹魏新城池樊城（6000/张郃）与上庸（4000/夏侯尚）；襄阳调整为守军 7000、守将曹仁。

### 变更
- 出征兵力与守城守军一律改用**有效兵力**；军事行动目标改为动态列出所有属曹魏的城池。
- 结算回合新增"大军日常消耗"记录；`打印局势()` 显示精锐比例与子任务标记。
- `计谋_劝降()` 增加城池参数，忠诚≥5 统一提示"守将忠心耿耿，劝降无望"。

### 修复
- 无。

### 测试
- 自测由 25 项扩展到 **37 项**：新增有效兵力、子任务结算、新城池数据、劝降提示等断言。

---

## [v1.0] — 2026-08-17（MVP 初始版本）

### 新增
- 纯标准库单文件游戏：三段式模块结构、20 回合主循环（清屏 + 打印局势 + 暂停 0.5 秒）、结局判定、自动演示模式。
- 军事三判定（强攻 >守军×1.5 / 围攻 >×0.8 / 必败 <×0.5 禁止送死）、死士突袭二选一、城破伤亡确定化（撤退 / 死守 / 默认驻守）。
- 6 种计谋（侦查粮道、劫粮道、侦查风向、火攻、据险而守、劝降），全部为前置条件判定。
- 连锁事件（东吴忌惮与割地抉择、3 回合后曹魏增兵）与阈值事件（魏军压境、东吴闭粮、国势衰微）。
- 自测脚本 `自测_确定性判定.py`（25 项断言，含"全文件不含 random 字样"的零随机校验）。

### 变更
- 无（初始版本）。

### 修复
- 无。

### 测试
- 25 项断言全部通过；自动演示 20 回合达成"汉家尚有可为"好结局。
