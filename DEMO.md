# 演示脚本 —— 三国 · 蜀汉突围

> 用途：面试/评审现场按时间盒走一遍，或对方自行照抄命令复现。
> 所有命令都在仓库根目录执行（Windows 用 `python`，macOS/Linux 可用 `python3`）。

---

## 一、现场演示（2~3 分钟）

时间分配：介绍 15 秒 → 启动 20 秒 → 玩法 60 秒 → 测试 40 秒 → 工程结构 40 秒 → 收尾 15 秒。

### 0. 一句话介绍（15 秒）

> "这是一个纯 Python 标准库实现的三国策略游戏原型，主程序约 1450 行、单文件三段式结构。
> 它最特别的地方是**零随机**——所有军事判定和计谋成败都由前置条件决定，玩家算的是'值不值得'，
> 而不是'赌不赌得到'。工程上做了配置数据化、版本化存档和一套可一键运行的测试体系。"

### 1. 启动与命令行（20 秒）

```bash
python main.py --version      # 工程发布版本 v1.0.0（游戏规则版本 v0.3.0）
python main.py --help         # 全部命令行参数
python main.py --demo         # 无人值守自动演示：20 回合，约 12 秒跑完
```

要讲的一句：**"发布版本和规则版本是分开记录的——工程迭代到 v1.0.0，但游戏规则本身一直停在 v0.3.0，因为这几轮只做工程化，没动过数值。"**

观察 `--demo` 输出里的几处关键信息（可指出）：

- 顶部一行为当前局势：`建安十三年九月 ｜ 第 1 / 20 回合`（回合以建安年月推进）；
- 中部的【战区态势图】：`★洛阳★（曹魏首都）`、`【蜀】永安◆（赵云·镇守）`、险关★、汉水分隔魏蜀；
- 【近事】里的月份前缀与确定性事件（如 `【冬至】寒冬已至…`）。

### 2. 核心玩法演示（60 秒，交互模式）

```bash
python main.py                # 输入 1 进入"扮演幕僚"
```

按顺序输入以下菜单项（每回合只能执行一件"大事"）：

| 输入 | 演示要点 | 讲什么 |
| :--- | :--- | :--- |
| `1` → `<将领序号>` → `1` → `3` | 把关羽设为"镇守城池 + 子任务练兵" | 将领任务直接决定防御加成与产出；子任务提升精锐比例 |
| `3` → `4` → `<将领序号>` → `1` | 强攻"上庸"（守军 4000） | 系统先给确定性判定：`强攻必胜！…预计损失兵力 X`，**没有概率**，只有算账 |
| `4` → `1`（侦查粮道） | 花 1 回合 + 10 粮草买情报 | 计谋不是掷骰子：**先花资源拿前置条件**，拿到就 100% 成功 |
| `7` → `1` → `demo1` → `0` | 存一个档 | 存档机制；之后可用 `--load demo1` 续局 |
| `6` | 结束本回合 | 系统结算 13 步（曹魏增长 → 粮草与冬季 → 将领任务 → 东吴骑墙 → … → 东吴均势外交） |

演示结束后如需查看战果：

```bash
python main.py --list-saves          # 列出存档与状态
python main.py --load demo1          # 从刚才的存档继续同一局（会提示恢复到第几回合）
```

> 想现场演示"改数值不改代码"：把 `config/diplomacy.json` 里 `亲蜀.好感调整` 由 `3` 改成 `7`，
> 再跑 `python main.py --demo`，日志/近事里的好感增量立刻变成 `+7`，改回即恢复。

### 3. 测试演示（40 秒）

```bash
python run_tests.py --fast        # 分类用例：约 11 秒，输出 总数/通过/失败
python 自测_确定性判定.py          # 原自测脚本：126 项断言，输出"全部 126 项自测通过。"
python run_tests.py               # 一键全量（含旧自测回归）：约 82 秒
python -m unittest discover -s tests -p "test_architecture.py" -v   # 只跑零随机/架构检查
```

要讲的三点：

1. **测试是分层的**：`tests/` 下按规则、边界、结局、回归、架构分文件；旧自测 126 项一点没删，被 `test_legacy_suite.py` 整体包装进一键入口。
2. **回归用例对应真实缺陷**：例如 M3 曾把"敌方城池的守军字段"误用到蜀汉城池（后者用"兵力"），导致**所有存档被判损坏**——现在有一条专门的回归用例盯着它。
3. **零随机是架构级约束**：架构用例逐行扫描生产代码，命中随机关键字就报 `文件:行号`，还检查"不得导入随机库""仅标准库依赖""同输入结果稳定"。

### 4. 工程结构演示（40 秒）

```bash
python main.py --check-config   # 配置校验：非法配置给出「文件 + 字段 + 期望 + 实际」
python main.py --demo --log-level INFO   # 日志（stderr）：启动/配置/每回合关键状态/结局
```

指给对方的四层结构（配合 `docs/架构图.md`）：

| 层 | 文件 | 一句话 |
| :--- | :--- | :--- |
| 入口与日志 | `main.py` → `src/蜀汉突围.py`（argparse + logging） | 命令行参数、日志级别；不带参数即原交互启动 |
| 规则与状态 | `src/蜀汉突围.py` 模块1/2/3 | 状态字面量、确定性判定纯函数、主循环 13 步流水线 |
| 配置层 | `config/*.json` + `src/config_loader.py` | 兵种/计谋/季节/外交/战斗五类常量可改，非法配置明确报错并整体回退 |
| 存档层 | `saves/*.json` + `src/save_manager.py` | 版本化 JSON 存档、损坏检测、原子写入、`--load` 续局 |
| 测试体系 | `tests/` + `run_tests.py` + `自测_确定性判定.py` | 一键跑通、分类清晰、旧断言不丢 |

### 5. 收尾（15 秒）

> "这个项目里我做的是：需求拆解与规则设计、数值与事件链定义、工程化四步（配置数据化 → 存档 → 测试体系 → 发布），
> 以及每轮迭代的验收与回归；代码由 AI 结对实现，我负责定标准、验收和把关。
> 现在这个仓库别人 clone 下来，按 README 三条命令就能跑起来、跑通测试。"

**现场问答备用**（都能在仓库里指出来）：为什么零随机？→ 见 `docs/规则说明.md` 与 `docs/架构说明.md`；
改数值要动代码吗？→ 不用，改 `config/*.json`；存档坏了会怎样？→ 明确报错并回退新开局，不崩溃；
测试跑多久？→ 快速 11 秒 / 全量 82 秒。

---

## 二、作品集演示视频脚本（1~2 分钟，含 AI 分工口播）

> 用途：随简历投递 / 上传平台的**作品集视频**。
> 目标：让对方在 2 分钟内相信两件事——
> ① 这是一个**真能跑、真有测试**的项目；② 我具备**用 AI 从 0 到 1 推进项目并为之负责**的能力。
>
> 拍摄建议：一次录屏到底即可（不剪辑也能用）。终端用深色主题、字号调到 16~18、
> 窗口高度够放下 30 行；只在两处需要输入命令，其余时间让它自己跑。

### 完整版分镜（约 1 分 50 秒）

| 时间 | 画面 / 操作 | 口播（可直接念） |
| :--- | :--- | :--- |
| 0:00–0:10 | 资源管理器或终端展示仓库目录树（`src/ config/ tests/ docs/`） | "这是我用 AI 辅助开发的三国策略游戏原型，纯 Python 标准库，零第三方依赖，从玩法设计到测试体系完整跑通。" |
| 0:10–0:25 | 敲 `python main.py --demo`，让它自己推演 20 回合 | "先看它能不能跑。这是无人值守自动演示：20 个回合、13 步结算，最后会给出结局。**全程没有任何随机数**——同一个局面、同一个决策，结果一定相同。" |
| 0:25–0:35 | 画面停在【战区态势图】与结局 | "这是它自己渲染的 ASCII 战区态势图，蜀魏城池、守将、险关、首都洛阳都在上面。玩法规则和这套数值平衡**是我自己设计的**。" |
| 0:35–0:50 | 敲 `python run_tests.py --fast`，等它输出 74/74 | "再看测试。一键入口，11 秒出结果：**74 个用例全绿**。这之外还有一份 126 项断言的历史自测脚本，一条没删，被整体包进这个入口里。" |
| **0:50–1:20** | ⭐ **关键镜头**：在 `src/蜀汉突围.py` 末尾加一行 `import random`，重跑测试 → 3 条失败，屏幕上出现 `src/蜀汉突围.py:1462: 出现随机关键字 'random'`；然后 `git checkout -- src/蜀汉突围.py`，重跑恢复全绿 | "这段是我最想展示的。我在需求阶段给 AI 定了一条硬约束：**这个项目不许有随机数**。但我没有靠'我记得'来保证——我要求它把这句话变成可执行的检查。现在我故意加一行 `import random`：三条架构测试立刻变红，还精确告诉我**哪一行**。还原之后又是全绿。**约束是机器强制的，不是口头承诺。**" |
| 1:20–1:32 | 把 `config/diplomacy.json` 的 `好感调整` 改成 `"三"`，跑 `python main.py --check-config` → 报错；再跑 `python main.py --demo` → 不崩溃、回退默认值；`git checkout` 还原 | "配置也是同理：数值全部抽到 JSON，改数值不用改代码；填错了它会说清楚**哪个文件、哪个字段、期望什么、实际给了什么**，并且整体回退默认值、绝不崩溃。" |
| 1:32–1:50 | 停在 README 的「AI 辅助开发说明」章节 | "最后说清楚分工：**玩法、规则、数值、验收标准是我定的**；**代码实现和测试代码主要由 AI 完成**；我负责每一轮的验收、提迭代要求、以及定下那些硬约束——比如不许引入第三方依赖、不许删掉任何一条已有断言。这两个真实缺陷就是测试先发现的，修完立刻写成了回归用例。**AI 写的每一行，我都为它负责。**" |

### 60 秒精简版（时间不够时用这 4 个镜头）

| 时间 | 镜头 | 保留的口播重点 |
| :--- | :--- | :--- |
| 0:00–0:12 | 目录树 | 是什么 + 零第三方依赖 |
| 0:12–0:30 | `python main.py --demo` | 能跑、零随机、20 回合 → 结局 |
| 0:30–0:42 | `python run_tests.py --fast` | 74 个用例全绿 + 126 项旧断言一条没删 |
| 0:42–1:00 | ⭐ 注入 `import random` → 3 条红 → 还原 → 全绿 | "约束是机器强制的" + 一句话分工说明 |

### 拍摄用命令清单（按顺序复制）

```bash
# ① 展示结构
ls                      # macOS/Linux；Windows 用 dir

# ② 能跑 + 零随机
python main.py --demo

# ③ 测试全绿
python run_tests.py --fast

# ④ 关键镜头：让"零随机"约束当场失效给你看
#    在 src/蜀汉突围.py 末尾手动加一行：
#        import random  # 演示用
python run_tests.py --fast        # 期望：3 条失败，并指出 src/蜀汉突围.py:<行号>
git checkout -- src/蜀汉突围.py    # 还原
python run_tests.py --fast        # 期望：恢复 74/74 全绿

# ⑤ 配置校验（可选）
#    把 config/diplomacy.json 的 亲蜀.好感调整 改成 "三"
python main.py --check-config     # 期望：报出文件/字段/期望/实际，退出码 1
python main.py --demo             # 期望：打印警告后回退默认值，照常跑完 20 回合
git checkout -- config/diplomacy.json
```

> **演示前务必确认工作区干净**（`git status` 应无输出），这样 `git checkout -- <文件>` 才能一键还原；
> 演示结束后再 `git status` 复核一次，避免把演示用的注入代码误提交。

### 已实测的真实输出（口播时可放心引用）

注入 `import random` 后，`python run_tests.py --fast` 的真实结果：

```
FAIL: test_架构_生产模块不含随机调用 (test_architecture.架构测试.test_架构_生产模块不含随机调用)
FAIL: test_架构_生产模块不导入随机库 (test_architecture.架构测试.test_架构_生产模块不导入随机库)
FAIL: test_架构_生产模块仅使用标准库与本地模块 (test_architecture.架构测试.test_架构_生产模块仅使用标准库与本地模块)
总数：70 ｜ 通过：67 ｜ 失败：3 ｜ 错误：0 ｜ 跳过：0

AssertionError: ["src/蜀汉突围.py:1462: 出现随机关键字 'random' → import random  # 演示用：故意注入"] != []
生产代码导入了随机库：src/蜀汉突围.py: import random
出现非标准库/非本地模块导入：src/蜀汉突围.py: random
```

把 `亲蜀.好感调整` 改成字符串 `"三"` 后，`python main.py --check-config` 的真实结果：

```
[配置校验] 未通过
检测到 1 处配置问题：
  · config/diplomacy.json → 字段 "外交.亲蜀.好感调整"：期望 0~100 之间的整数，实际 '三'（字符串）
  提示：修改 config/ 下对应文件后重新运行；或执行 python config_loader.py 单独校验；删除 config/ 可回退内置默认值。
（退出码 1）
```

而直接运行 `python main.py --demo` 时，配置非法**不会崩溃**：打印警告、整体回退内置默认值，照常跑完 20 回合并以退出码 0 结束。

### 截图与图片素材

需要补的截图清单（文件名、拍什么、用什么命令复现）见
[README「运行截图与演示视频」](README.md#九运行截图与演示视频)一节，图片统一放在 `docs/images/` 下。
